create database bazablog;
use bazablog;
create table user(
	id int auto_increment primary key,
    login varchar(12),
    haslo VARCHAR(300)
);
alter table user add email varchar(100);
select * from user;